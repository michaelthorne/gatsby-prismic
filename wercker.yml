box: node
build:
  steps:
    - script:
        name: install dependencies
        code: |
          yarn install
          cd functions; yarn install
    - script:
        name: build gatsby
        code: gatsby build
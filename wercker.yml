box: node
build:
  steps:
    - script:
        name: install dependencies
        code: |
          npm ci
          cd functions; npm ci
    - script:
        name: build gatsby
        code: gatsby build